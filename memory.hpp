#ifndef __MEMORY_HPP
#define __MEMORY_HPP

namespace memory
{
    constexpr HEAP_MIN_SIZE = 32 * 1024;

    void* contiguous_allocator()
    {
        auto heap_max = []() { return }
    }
}



#endif // __MEMORY_HPP
